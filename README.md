Each folder contains a different iterations of the model (either corrections, something I wanted to test out). 
* soc_min_rule_continuous: contains the continuous version of the soc model
* two_species_defector: contains the two species min rule
* ll_2D_and_volume_exclusion: contains some other versions and benchmark tests
